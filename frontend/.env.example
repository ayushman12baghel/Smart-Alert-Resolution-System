# Copy this file to .env.local for local development (Vite proxy handles /api/* automatically,
# so VITE_API_BASE_URL is intentionally left blank for dev).
#
# For production (Vercel), add this variable in Vercel's Environment Variables settings:
#   VITE_API_BASE_URL = https://your-app.onrender.com
#
# Do NOT commit .env.local to source control.

VITE_API_BASE_URL=
